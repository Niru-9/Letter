# Port — Render sets this automatically, leave as 4000 for local dev
PORT=4000

# Optional: extra frontend origin to allow (Render already has 143letters.vercel.app hardcoded)
FRONTEND_URL=https://143letters.vercel.app
